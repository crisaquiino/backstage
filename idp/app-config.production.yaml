app:
  baseUrl: http://localhost:3000

backend:
  baseUrl: http://localhost:7007
  listen:
    port: 7007
  database:
    client: pg
    connection:
      host: ${POSTGRES_HOST}
      port: ${POSTGRES_PORT}
      user: ${POSTGRES_USER}
      password: ${POSTGRES_PASSWORD}

azureDevOps:
  organization: embraer-cloud
  project: eve-platform
  token: ${AZURE_DEVOPS_PAT}
  reviewerId: ${REVIEWER_ID}